{
  "_README": "Controls no e2e suite presses, each with the reason. This list is a RATCHET: it is seeded with what was untested when the audit was added, so it does not demand retroactive coverage — but a control added later has no entry, so CI fails until someone tests it or writes down why not. Deleting an entry and adding coverage is always the better move. src/controlcoverage.test.ts also fails on a STALE entry, so a renamed control cannot keep a reason that no longer protects anything.",
  "Close Dialog": "Same. NOTE: this and 'Close dialog' both exist — the app spells the same aria-label two ways, which is worth unifying. The lowercase spelling (the Account dialog's own close button) is now exercised by RED-APP-8/005's e2e fixture (its geometry reliably discriminates the max-h fix, unlike the Login submit button); this capital-D spelling is elsewhere and still untested.",
  "Done": "Dialog chrome: dismisses without side effects, and failure is visible immediately.",
  "Log out": "Token invalidation is covered end to end by src/integration/api.test.mjs against the real server, which is a stronger test than clicking the control that triggers it.",
  "Sign In to Your Account": "See 'Log out'.",
  "Increase x₀": "The x0/y0 steppers. The VALUE path they drive is covered — smoke types into x0 and asserts the log opens at 'Start (0.000, …)', and the mobile suite asserts these meet the 24px WCAG 2.2 AA floor after they were found at 25x20. What is untested is the increment arithmetic itself.",
  "Decrease x₀": "See 'Increase x₀'.",
  "Increase y₀": "See 'Increase x₀'.",
  "Decrease y₀": "See 'Increase x₀'.",
  "Prefer the terminal?": "Informational link-style control; navigates, holds no state.",
  "Get the desktop app": "Starts a DMG download from GCS. Not clicked in CI because the download itself is the thing it does; the release pipeline covers producing and uploading that artifact.",
  "Sign out": "See 'Log out'.",
  "Dismiss": "OtherAccountsNotice's own close button — sets a localStorage flag and hides the banner, no server call, no other state. The banner's SERVER-SIDE predicate (GET /api/auth/desktop-hint) is covered end to end by src/integration/desktop-recovery-hint.test.mjs against the real server; this button is client-only dialog chrome on top of it, same shape as 'Cancel'/'Done' above."
}
