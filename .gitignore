node_modules
build/icon.iconset/
dist/
coverage/
.DS_Store
*.log
.env
.env.local
.env.*.local
db.json
eval-results.json

# Electron build and packaging files
dist-electron/
*.app/
*.dmg
*.zip
*.blockmap

# Local agent / tooling artifacts
CLAUDE.md
AGENTS.md
CODEX.md
.codex/
.claude/
.playwright-mcp/
_gen/exemplars/*.json
.freebuff/

# SoME submission description — SoME was dropped as a venue (2026-08-24).
# Untracked from the tree on 2026-08-29; it remains in history at cfc7637 and
# three later commits, which was accepted rather than rewriting public main.
SOME_DESCRIPTION.md
# session handoff files travel by hand, never by git
handoffs/
handbacks/
# CLAUDE.md is local (line above); its session-history half lives beside it, also local
CLAUDE-SESSION-HISTORY.md
CLAUDE.md.bak*
